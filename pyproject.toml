[tool.black]
line-length = 88
target-version = ['py310']

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 88
exclude = ["**/migrations/**", "staticfiles_build/**", "static/**", "node_modules/**", ".venv/**", "scripts/**"]
